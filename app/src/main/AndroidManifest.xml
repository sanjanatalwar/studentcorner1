<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.auribises.college">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Button_Shape" />
        <activity android:name=".Student_Registration" />
        <activity android:name=".EdittextShape" />
        <activity android:name=".TeacherRegistration" />
        <activity android:name=".AllStudentActivity" />
        <activity android:name=".TeacherMarksUpdate" />
        <activity android:name=".TeacherOption" />
        <activity android:name=".AllBcaTwoStudents" />
        <activity android:name=".UpdateBca2" />
        <activity android:name=".AllBcaThreeStudents" />
        <activity android:name=".UpdateBca3" />
        <activity android:name=".SlideShowActivity"></activity>
    </application>

</manifest>